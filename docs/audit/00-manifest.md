# Verza TV — Audit Manifest (Wave 0)

Generated by repo enumeration at commit `83c29d1`. This is the audit's
**coverage denominator**. Every agent reports coverage as a fraction of it, and an
item it cannot account for is reported as a gap rather than skipped.

## The denominator

| Class | Count |
|---|---|
| Route instances (pages + API + every resolved show and episode URL) | **5129** |
| Interactive elements (links, buttons, anchors, inputs, handlers) | **535** |
| i18n cells (115 keys x 20 locales) | **2300** |
| External links and third-party embeds | **74** |
| **Total auditable items** | **8038** |

Catalog rows (96) are counted inside route instances and listed
separately below because five of them behave differently from the other 91.

## Routes

- **65 page routes**, **55 API routes**, **0 file routes** (sitemaps, robots, og images)
- **96 show pages** (91 prerendered; the 5 coming-soon rows render on demand)
- **4913 episode routes** (2214 prerendered, capped at 25 per series; the remaining 2699 render blocking on first request)

## Catalog

96 rows: **1 live**, **0 coming soon**.

Expected routing under the restored rule (playable plays, unplayable explains):
every live row resolves to `/series/<slug>/1`, every coming-soon row to
`/series/<slug>`. Per-row expectations are in the JSON at `catalog.detail`.

## Interactive elements

535 total: 183 link, 125 button, 163 handler, 52 input, 12 anchor.
Each carries a stable `file:line` identifier in the JSON.

## i18n

20 locales: en, es, fr, pt, de, it, ja, ko, zh, hi, ar, ru, tr, pl, nl, th, vi, id, tl, sw.
115 keys. Every key must exist in every locale with its
interpolation tokens intact.

## Rate limit tiers

- `/^\/api\/ai-host/` — 5/min
- `/^\/api\/push\/send/` — 10/min
- `/^\/api\/checkout/` — 15/min
- `/^\/api\/unlock/` — 15/min
- `/^\/api\/subscribe/` — 15/min
- `/^\/api\/auth\//` — 10/min
- `/^\/api\/access/` — 120/min
- `/^\/api\/playback\//` — 90/min
- `/^\/api\/watch-progress/` — 60/min
- `/^\/api\/events/` — 180/min
- `/^\/api\//` — 30/min

The bucket key is `${ip}:${limit}`, so tiers sharing a limit share a bucket.

## External links and third-party surfaces

74 distinct external URLs. Each must be crawled, not read.

## Known-open going in — UNVERIFIED until this run confirms them

- Agent C status: $1.99 buy path, guest-purchase attachment, restore purchases, five paywall fixes, Amazon pill, Shop pricing, VIP
- No purchase has ever been completed on this platform - checkout unverified by definition
- iOS/IAP architecture decision pending
- Duplicate synopses across ~1/4 of the Drama catalog
- Free-run chip correctness on the 5 wholly-free titles and 2 clamped titles
- Preview burnable without watching - gating on watch time vs swipe count unconfirmed
- Mux audio tracks LANGUAGE=und (needs Mux write credentials)
- Two false comments in lib/catalog.ts (blocked by source fingerprint)
